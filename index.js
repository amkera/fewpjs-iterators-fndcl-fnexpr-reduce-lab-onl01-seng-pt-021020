const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

const totalBatteries = batteryBatches.reduce(function(amber, batches){
  return batches + amber
  }
  //for each time we  iterate through batteryBatches, add the element  to the total 
)